<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter needs it for development (hot reload over localhost). -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
